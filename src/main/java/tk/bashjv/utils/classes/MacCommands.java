/*
 * Develop by unbegrenzt at 2 - 2021.
 */

package tk.bashjv.utils.classes;

import tk.bashjv.utils.enums.BashCommand;
import tk.bashjv.utils.intefaces.ConsoleCommands;

/**
 * The type Mac commands.
 * Commands to execute on osx based systems.
 */
public class MacCommands implements ConsoleCommands {
    /**
     * @param bashCommand the bash command
     * @return the selected command string to be executed
     */
    @Override
    public String Command(BashCommand bashCommand) {
        return null;
    }
}